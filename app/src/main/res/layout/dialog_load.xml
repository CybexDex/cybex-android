<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/dialog_width_height"
    android:layout_height="@dimen/dialog_width_height"
    android:background="@drawable/bg_load_dialog">

    <ProgressBar
        android:id="@+id/load_progressbar"
        android:layout_width="@dimen/dialog_progress_width_height"
        android:layout_height="@dimen/dialog_progress_width_height"
        android:layout_gravity="center" />

</FrameLayout>